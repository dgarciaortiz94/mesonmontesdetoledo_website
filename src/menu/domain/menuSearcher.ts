import IMenuDataAccess from "@/menu/domain/iMenuDataAccess";
import Menu from "./menu";

export default class MenuSearcher {
    // private dataAccess : IMenuDataAccess;

    // public constructor(dataAccess : IMenuDataAccess) {
    //     this.dataAccess = dataAccess;
    // }

    // public async searchAll() : Menu[] {
    //     let response = this.dataAccess.searchAll();

    //     this.handleNotSuccessfulResponse(response);
    // }

    // public async handleNotSuccessfulResponse(response : Promise<Response>) {
    //     if (!(await response).ok) {
    //         await this.handleNotFoundErrors(response);
    //     }
    // }

    // public async handleNotFoundErrors(response : Promise<Response>) {
    //     if ((await response).status == 404) {
            
    //     }
    // }
}
